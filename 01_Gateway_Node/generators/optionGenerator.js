import path from 'path';
import { fileURLToPath } from 'url';
import dotenv from 'dotenv';

/**
 * returns an array of option chain, or array containing multiple arrays of option chain
 * @param {visibility} param0 in months recommended: upto 4
 */

async function getOptionChain({exchange, segment, pointsFromStrike, visibility}) {

}

export {
    getOptionChain,
}